[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-truck-interhack"
version = "0.1.0"
description = "Hybrid route + load optimizer prototype for the Damm Smart Truck challenge."
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["smart_truck"]
